import { Flex } from '@chakra-ui/react';

const ClientAuthentication = () => {
  return (
    <Flex bgColor={'light'} borderRadius={'lg'} h={'100%'} w={'100%'}></Flex>
  );
};

export default ClientAuthentication;
