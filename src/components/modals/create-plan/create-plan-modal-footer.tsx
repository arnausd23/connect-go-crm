import { Button } from '@chakra-ui/react';

const CreatePlanModalFooter = () => {
  return (
    <Button
      bgColor={'blue.400'}
      colorScheme={'blue'}
      fontSize={'14px'}
      variant={'solid'}
    >
      {'Crear'}
    </Button>
  );
};

export default CreatePlanModalFooter;
