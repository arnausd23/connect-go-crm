import { Button } from '@chakra-ui/react';

const AssignPlanModalFooter = () => {
  return (
    <Button
      bgColor={'blue.400'}
      colorScheme={'blue'}
      fontSize={'14px'}
      variant={'solid'}
    >
      {'Asignar'}
    </Button>
  );
};

export default AssignPlanModalFooter;
