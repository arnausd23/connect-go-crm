import { Button } from '@chakra-ui/react';

const SettingsModalFooter = () => {
  return (
    <Button
      bgColor={'blue.400'}
      colorScheme={'blue'}
      fontSize={'14px'}
      variant={'solid'}
    >
      {'Guardar'}
    </Button>
  );
};

export default SettingsModalFooter;
